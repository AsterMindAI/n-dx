# Team Jarrett — Backlog

Open work only. Shipped/merged work drops off. **This file is the source of truth for status and
claims** — a claim that lives in a chat message or an agent's head does not exist.

**Claim protocol:** set `Claimed by` + `Status → IN-PROGRESS`, then **commit**. First commit wins;
if you collide on rebase, pick another item.

**ID format:** `TJ-<AGENT INITIAL><n>` — e.g. `TJ-A1`.
**Status:** `PENDING` · `IN-PROGRESS` · `BLOCKED (<blocker id>)` · `ONGOING` · `DONE`

| ID | Item | Related ADR / IMPL | Status | Claimed by |
|---|---|---|---|---|
| TJ-A1 | ELM pre-filter stage before classify.ts's LLM fallback — prototype/eval phase. Numeric feature representation clears the gate (100% @ 59% coverage out-of-domain); independently confirmed by Knight's `TJ-K1` (97.0% @ 42.3% on the same held-out set) | `ADR-2026-08-11-jarrett-elm-prefilter-classify.md`, `IMPL-2026-08-11-jarrett-classify-elm-swap.md` | DONE — superseded by `TJ-A2` for production wiring | Archer |
| TJ-A2 | Production-harden the ELM pre-filter: model lifecycle, schema widening, real wiring into `runClassificationsPhase`, config kill-switch, test coverage. **Sole focus per the user's 2026-08-27 instruction** — `TJ-A3` reassigned away from Archer (see that row); Knight's continued involvement here is unconfirmed, may be reassigned too | `ADR-2026-08-11-jarrett-elm-prefilter-classify.md`, `IMPL-2026-08-23-jarrett-classify-elm-production-hardening.md` | IN-PROGRESS — steps 1-9 done (lifecycle, schema, wiring, kill-switch, unit+integration tests, 28 tests total). Shipped with `elmPrefilter.enabled` defaulted to **false**: a "zero-evidence population" gap means 100% of files reaching the pre-filter have all-zero feature vectors, now a *structural* guard in `classifyWithELM` (not just calibration luck) so no threshold override can bypass it. `pnpm build/typecheck/test` clean for `@n-dx/sourcevision`. Remaining: regression fixture (step 10), whole-repo build check (step 11), and — the real blocker — a feature representation that works on zero-evidence files, before `enabled` can flip to `true`. **That blocker now has a plan: `TJ-R2`.** **Note 2026-09-07: `classify.ts` is no longer untouched regardless — Team Thomas's `TT-N1` edited it directly and merged via `dev`; see `TJ-R2`'s row and `IN-FLIGHT.md`** | Archer |
| TJ-A3 | Extend/tighten the hand-curated archetype catalog (`BUILTIN_ARCHETYPES`) — see ADR/IMPL for the full evidence and plan | `ADR-2026-08-24-jarrett-archetype-taxonomy-redesign.md`, `IMPL-2026-08-24-jarrett-archetype-taxonomy-redesign.md` (executing) + `IMPL-2026-09-03-knight-tj-a3-execution-and-tj-r2-gate.md` (adds LLM-reasoning-mining as a second signal source, makes the `TJ-R2` sequencing explicit) | **Claimed by Knight, 2026-09-03** — unowned since the 2026-08-27 reassignment; picked up per the user's recommendation to shrink the zero-evidence population deterministically before further `TJ-R2` work. Not started yet | Knight |
| TJ-R1 | Strategic pivot: ELM becomes primary classifier for the hard-case population, LLM becomes true last-resort (not a narrow pre-filter) — decision + doc updates only, no new code. Triggered by triple-independent verification (Archer, Knight, Realm's reproduction of both agents' real committed eval scripts) | `ADR-2026-08-24-realm-elm-primary-classifier-pivot.md`, `IMPL-2026-08-24-realm-elm-primary-classifier-pivot.md` | DONE, threshold finding now provisional — see `TJ-A2` row. Decision itself (ELM as primary against whatever catalog exists) still holds; the specific number was measured against the pre-`TJ-A3` catalog | Realm |
| TJ-R2 | Fix `TJ-A2`'s zero-evidence-population gap: new ELM feature representation (path text + raw export names, not the always-zero evidence vector at this call site) validated specifically against each corpus's zero-evidence population, not "any unclassified file" like every prior eval | `ADR-2026-08-31-realm-path-based-elm-classifier.md`, `IMPL-2026-08-31-realm-path-based-elm-classifier.md` | **IN-PROGRESS, now double-gated** — Step 4 done (`extractPathExportExamples`/`pathExportVector`, real code + 10 new unit tests, committed `ae9dc463` in `../n-dx-jarrett`). Still soft-gated on `TJ-A3` (Knight) for the population-dependent eval. **New 2026-09-07: also soft-blocked on reconciling with Team Thomas's `TT-N1`** — same fix (path-text ELM), built independently, already merged into `classify.ts` on this branch (`107cd344`). Steps 5-6 on hold pending a joint decision — see `IN-FLIGHT.md` § 3/§ 4 and the cross-team note in `Thomas-Agents/Notes/`. **Which representation ships is now `TJ-R3`'s Out-of-scope item, not decided here**. **SUPERSEDED 2026-09-17 by `TJ-E1` (Elon), per the user's direction** — the representation question moves wholesale to `TJ-E1`, which reads full file content rather than path+export text alone. Archer's Step 4 work (`extractPathExportExamples`/`pathExportVector` + 10 tests, `ae9dc463` on `../n-dx-jarrett`) is **not discarded** — Elon absorbs it rather than rebuilding it. Both soft-blocks that held this item (Knight's `TJ-A3`, the `TT-N1` reconciliation) are moot: `TT-N1` is no longer on this branch | Archer → superseded by Elon |
| TJ-R3 | Reconciliation proposal for the `TT-N1`/`TJ-R2` collision: split `classify.ts` into a pure gate plus `classify-ELM.ts`/`classify-LLM.ts`, each with a stable interface, `classify.ts` as the only file that calls either. Doesn't decide which ELM representation wins — that stays open. Config-driven kill switch replaces `TT-N1`'s hardcoded constant | `ADR-2026-09-07-realm-classify-gate-split.md`, `IMPL-2026-09-07-realm-classify-gate-split.md` | **DONE — shipped 2026-09-07** (`7ecf69f3`, merged into `Jarrett` via `ae381889`). **Status corrected 2026-09-17 by Elon — this row said "PROPOSED — blocked on Thomas/Nala's sign-off" for 10 days after the code had landed.** Proceeding without Thomas's sign-off was an explicit user override, recorded in the commit message. Found en route: the `dev` merge had left `classify.ts` importing three symbols `classify-elm.ts` never exported — a broken build (3× TS2305), not just an architectural collision; the split fixed it and removed the dead inline `TT-N1` gate. **IMPL step 10 still open** — updating `TT-N1`/`TJ-R2` ADR statuses and closing the collision notes was left for Realm/Thomas rather than decided unilaterally | Realm (planned) / shipped by Jarrett |
| TJ-E1 | **The ELM classifier itself** — fill the empty representation slot behind `runELMGate()`. ELM-only, no LLM knowledge inside it: file in, confident archetype or nothing out. Reads the **full file — metadata and content** (new: nothing at this call site has ever read file text). Includes training-data extraction, model lifecycle, confidence-gate calibration from a measured precision/coverage curve, and the LLM-label retrain loop. **Supersedes `TJ-R2`** per the user's 2026-09-17 direction | ADR + IMPL to be written before code | **IN-PROGRESS** — onboarded 2026-09-17, worktree `../n-dx-elon`, branch `elm/jarrett/classify-elm-content`. No code yet. First task: absorb Archer's `TJ-R2` Step 4 encoder work (`ae9dc463` on `../n-dx-jarrett`), then write ADR + IMPL | Elon |
