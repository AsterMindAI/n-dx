# Team Thomas — syncs

Copy [`../../SYNC-TEMPLATE.md`](../../SYNC-TEMPLATE.md) to
`SYNC-<NNN>-YYYY-MM-DD-<slug>.md`. `<NNN>` is per-team, so it can't collide.

A **cross-team** sync is filed in the coordinating lead's `syncs/` and linked from the other
teams' `Notes/` inboxes.
