# Team Nolan — Backlog

Open work only. Shipped/merged work drops off. **This file is the source of truth for status and
claims** — a claim that lives in a chat message or an agent's head does not exist.

**Claim protocol:** set `Claimed by` + `Status → IN-PROGRESS`, then **commit**. First commit wins;
if you collide on rebase, pick another item.

**ID format:** `TN-<AGENT INITIAL><n>` — e.g. `TN-A1`.
**Status:** `PENDING` · `IN-PROGRESS` · `BLOCKED (<blocker id>)` · `ONGOING` · `DONE`

| ID | Item | Related ADR / IMPL | Status | Claimed by |
|---|---|---|---|---|
| TN-N17 | **Provenance defect found and fixed: the corpus recorded the wrong commit for 41% of its rows.** `elm-corpus-build.mjs` recorded `git rev-parse HEAD` **at build time**, but labels come from `.sourcevision/classifications.json`, written by an **earlier `analyze` run**. For the eight staged clones these agree; **for n-dx they never did** — corpus v2 attributes n-dx's **255 of 624 rows** to `90e5bdb7` while the analysis that produced them ran at **`b8770042`**, twelve days earlier (verified against `.sourcevision/manifest.json` `gitSha`). **Benign in fact — none of the 255 labelled files changed between those commits, verified — so the corpus is sound and only the provenance was wrong.** Fixed: every repo now records **both** commits plus `analyzedAt`, and the builder reports `ANALYSIS != BUILD`, a missing analysis commit, or a genuinely dirty source tree (ignoring `.sourcevision/` and `.n-dx.json`, which are our own scaffolding — counting them made the warning fire on every correctly-prepared repo). **This is the guard Jam asked for before the harvest, keyed on the commit that actually describes the labelled tree rather than the one Jam proposed.** | Jam's 2026-09-18 note § 5 | DONE | Nutella (Team Nolan) |
| TN-N18 | **Callgraph and component metadata: deferred by the lead, not dismissed.** `sourcevision` emits six artifacts; this design uses two. The unused per-file ones are substantial and free — **6,277 functions with `isExported` and 180,237 call edges** on n-dx, plus a component catalog with `serverRoutes`/`routeModules`. Jam measured an export ratio that orders sensibly by archetype (route-handler **0.30** → schema **0.86**; **`gateway` files declare 0.0 functions** — this repo's architectural rule made measurable, cleaner than the `reexportRatio` already fed). **Withheld by this ADR's own rule:** they exist for only **2 of 9** repos, because every harvest run stopped after phase 3 and phases 5–6 never ran — the identical evidentiary state that got `zone` withheld. **Backfilling costs ZERO LLM calls** (`analyze <repo> --only=callgraph`), so waiting costs nothing and adding now would be the first exception to a rule that has caught four bad features. Added to the withheld tables in the ADR and `FEATURES.md` as deferred-not-dismissed, alongside `symbols[]`. | Jam's 2026-09-18 note §§ 1-2 | DEFERRED | _lead's call 2026-09-18_ |
| TN-N19 | **Two defects in `packages/sourcevision/**` — found by Jam, and neither of us owns that path.** (a) **`analyze --help` is stale:** it advertises `--phase=<N>` as "1–4" and omits `callgraph` from `--only`, while the code runs **six** phases — anyone reading help would conclude the callgraph phase does not exist, which is most of why nobody collected it. (b) **n-dx's own `.sourcevision/manifest.json` records `zones: "running"`** while `zones.json` exists on disk — a stuck status from an interrupted run. Harmless in itself, but **the manifest is exactly what we now rely on to tell us which phases ran**, and `TN-N17`'s provenance gate reads it. Filed rather than fixed: that path is Team Jarrett's territory and on nobody's claim. Needs a cross-team note. | Jam's 2026-09-18 note § 6 | PENDING | _needs the lead — not Team Nolan's path_ |
| TN-N15 | **The deliverable repo is PUBLIC (lead, 2026-09-18), and that changes what the warranty is for.** `ELM-database-ndx` is no longer a private hand-off to two known teams who have read our notes — **public is permanent**, and anything shipped can be trained on by a stranger who will never read a note and will reasonably blame the data when it behaves like corpus v1 did. **`FEATURES.md` and the `ELM-CORPUS.md` §§ 3a/5a/6/7 warranty stop being courtesy and become the load-bearing half of the deliverable.** Specifically, a public consumer must be unable to miss: the teacher is **72.3% against truth** and is **not shown the path only**; `residue` is the rules' leftovers, not the repo; only `role: "source"` is ever classified; **v2 coverage is 28.0% on fresh ecosystems, K1′ FAIL**; and block scaling is theirs to choose. Nothing ships until the document ships with it. | `ADR-2026-09-17-nutella-…` § 6 | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N16 | **`TN-J22` deferred by the lead — recorded with its trigger condition so "later" is knowable.** The classify prompt is **the only lever on label quality**: the teacher sits **13.1 pp below the human path-only ceiling** (72.3% vs 85.4%), and *nothing in the database scope changes it* — a better student cannot fix a 28%-wrong teacher. **The condition under which it arises:** if the tier plateaus below 85.4% after the feature space is fixed, the teacher is the ceiling being hit, not the corpus size and not the features. Deferred, not dropped. | `K2-HANDBOOK.md` § 8 | DEFERRED | _lead's call 2026-09-18_ |
| TN-N14 | **IMPL up for the database build — needs the lead alongside the ADR.** Six phases, and the spine is a sequencing decision: **measure the ADR's premise before harvesting anything.** Verified 2026-09-18 that this costs **zero LLM spend** — all 7 training repos have `inventory.json`/`imports.json` on disk, **100% of gold set #2's 250 evaluation paths join to inventory** (hono 81/81, trpc 169/169), and the coverage check needs no labels, so the blind set can be re-measured indefinitely without being spent. Phase 0 **pre-registers the bar before any model runs** (population, operating point, metric, and the committed **28.0%** baseline). Phase 2 is a **parity control**: the new feature-space harness must reproduce 33.8/28.0/35.8/24.3 on the frozen v2 model **before it may report any number** — a new instrument that cannot reproduce the old instrument's result is not measuring the same thing. Phase 3 selects normaliser and block scale on **train-CV only**. **Phase 5's harvest is conditional on Phase 4**, and if structural features do not beat 28.0% the plan is to **publish the negative and not harvest** — the last time this project harvested first and diagnosed second it spent a labelling day on a feature-space problem. ⛔ Does not modify `elm-coverage-check.mjs` (Jam's, and it produced the baseline), regenerates no existing corpus, and touches no `packages/**`. | [`IMPL-2026-09-18-nutella-elm-training-database-construction.md`](../IMPL/IMPL-2026-09-18-nutella-elm-training-database-construction.md) | **IN-PROGRESS — Phase 0 and 1 done, Phase 2 running** | Nutella (Team Nolan) |
| TN-N13 | **ADR up for the construction of the new database — needs the lead.** Two datasets over a shared **three-layer row** (`identity` / `label` / `raw` / `features`), the **import graph and inventory committed whole** as companion artifacts rather than flattened into columns, and **every model-facing scale quantity normalised within-repo**. Rests on four measured findings, all reproducible via the committed `scripts/elm-feature-survey.mjs`: (1) mean in-degree spans **0.59 → 6.04, a 10.2× spread**, so raw degrees rebuild the v1 repo-prior; (2) **305 of 341 external packages are single-repo**, only 11 appear in ≥3 of 9, so a package one-hot is path tokens again — use a curated family map; (3) a fifth edge type **`require`** (express 100%, fastify 91.6%) means edge-type mix encodes **CommonJS vs ESM**, so collapse `static`+`require` and keep `type` separate; (4) **`zones.json` absent for 7 of 9 repos** and `category` repo-specific, so both withheld. **Re-validation corrected the ADR twice** — the zones count (I had said 1 of 10; it is 2 of 9, having omitted n-dx itself) and `role`, which moved to withheld once the survey showed it **constant `source` on 536 of 536 harvested rows**. **No model trained, no accuracy claimed, nothing harvested, no LLM calls spent.** | [`ADR-2026-09-17-nutella-elm-training-database-construction.md`](../ADR/ADR-2026-09-17-nutella-elm-training-database-construction.md) | **REVISED 2026-09-17 after Jam's review — six changes, three of which alter a decision.** § 3a added: **block scale is now a declared contract** (the 12-scalar structural block carries **~2.7×** the energy of the entire 2,890-dim path block; reproduced independently). **The normaliser is no longer presented as settled** — percentile imports repo composition, `inDegree: 1` is the **12.5th** percentile in n-dx and the **72.7th** in commerce, so `log1p` and pooled global quantiles are named as live alternatives decided by measurement. **`language` and `depthFromRoot` moved to withheld** — all 11 `Vue` rows are one repo and all 11 are `component`, i.e. a repo fingerprint that is also a perfect in-sample label predictor. **`symbols[]` added to the withheld table** as deferred-not-dismissed. **Hiccups 1, 2, 5 corrected.** **Two stale zone counts fixed** that had survived my own correction in the same document. | PENDING | _needs the lead_ |
| TN-N9 | 🔴 **`ELM.train()` does not train on what you pass it — and the repo's own onboarding proof is void.** The base `ELM`'s text-mode `train()` is `train(augmentationOptions?, weights?)` (`dist/core/ELM.d.ts:59`); the implementation (`dist/astermind.esm.js:1146`) iterates `this.categories` and trains on character variants of the **category label strings**. An array of training rows is read as `augmentationOptions`, every property is `undefined`, and it silently uses defaults. **No error.** **Proved by execution** — reproducing `elm-hello-world.mjs`'s own held-out eval, varying only the argument: real 30-path set **5/6 (83%)**, inverted labels **5/6**, empty array **5/6**, no argument at all **5/6**, identical predictions every time. The script still prints `OK — library loads, trains, and generalizes`. It scores 83% because the labels `route`/`component`/`test` match the held-out directory names. **Blast radius checked script by script: VOID are `elm-hello-world.mjs` and `scripts/elm-prototype/*` (via `classifier.mjs:59`). NOT affected** — `elm-architecture-sweep`, `elm-certify`, `elm-coverage-check`, `elm-diagnostics`, `elm-feasibility-screen`, `elm-freeze-model`, `elm-goldset2-power`, `elm-k2-analysis`, `elm-operating-point`, all of which use `trainFromData`; the two `.train()` calls at `elm-architecture-sweep.mjs:200` and `elm-operating-point.mjs:262` are on `VotingClassifierELM`/`ConfidenceClassifierELM`, whose `train()` takes real data. **So the tier's headline numbers stand** (`tanh` +4.0 pp, 4096, the operating points, 72.3%/85.4%/54.4%). ⚠️ **`claude-context-instruction` § 1 and `NEW-AGENT.md` Step 1 both still point every new agent at that script as proof the library works** — those are shared/Fluff-owned files, so this is filed, not edited. Found by Team Thomas (Nala) first, relayed by Syrup, verified here. | `ELM-CORPUS.md` § 10a | PENDING | _needs the lead — shared files_ |
| TN-N10 | ⚠️ **A v2 coverage number is circulating with no artifact behind it.** Syrup's 2026-09-16 note § 8 states corpus v2 reaches *"28.0% on fresh ecosystems … still under the 30% bar"*. **There is no committed coverage artifact for v2, no recorded seed, and no recorded script invocation** — I searched `scripts/data/`, the findings ledger and the full git history; the only other `28.0` in the repo is `undici-types: ^6.28.0`, a coincidental substring. **Jam, who holds `TN-J32`, states the check is unrun** and additionally that it **OOMs** on the v2 model. Two teammates disagree about whether the single most important measurement on this project exists. It may genuinely have been run locally — if so it needs committing with seed and baseline. Until then **nobody should quote it**, and v2's central property (that it generalises) remains untested. *If it IS 28.0%, that is under the 30% bar — a FAIL, and it changes strategy.* | `ELM-CORPUS.md` § 6 | **AUTHORISED 2026-09-17 by the lead: Jam runs the v2 coverage check.** Jam has stated for the record that they **never ran it, never produced a v2 coverage number, did not originate 28.0% and cannot source it** — so the figure has no author, no artifact, no seed and no invocation. Jam's narrowing evidence: the check **OOMs at node's default heap** (recipe-not-weights → nine 4096-unit refits, dies ~1978 MB vs ~2096 MB), so **anyone who ran it successfully must have passed a non-default `--max-old-space-size`** — a specific thing to have done and an odd one to omit. Invocation: `node --max-old-space-size=6144 scripts/elm-coverage-check.mjs --frozen=scripts/data/elm-frozen-model-v2.json` — 20–30 min compute, **no labels, no LLM spend**, artifact committed either way. ⚠️ **Until that artifact exists nobody quotes 28.0%**, and the ADR still quotes no v2 coverage figure. | **RESOLVED 2026-09-18 — Jam ran it and the number was right to the decimal.** `scripts/data/elm-coverage-v2.log` is committed with its full invocation, input hashes and library version: **33.8% trained-on (PASS) → 28.0% fresh (K1′ FAIL)**, hono 35.8% PASS, trpc 24.3% FAIL. **Syrup measured correctly and reported accurately**; the only real gap was a number without a committed invocation, and that is now closed. Jam has retracted, in place, the suggestion that the figure was "narratively convenient". ✅ **28.0% is now the committed baseline for every successor model.** | DONE | Jam (Team Nolan) |
| TN-N11 | **Row-schema design decision, before any harvest at scale.** The lead's diagram has the ELM looking at **extension, content, and name**. **Nothing built so far looks at content** — the algorithmic pass reads filename + directory strings (74 of 75 signals in `archetypes.ts`), the LLM half sees `` `${i+1}. ${f.path}` `` (`classify-llm.ts:192`), and the corpus stores `text: fc.path`. Every number this project has published is a path-only number, **including the LLM's own 72.3% against truth**. **Raw file text is collected nowhere**, so content is new collection and it is mine to design and budget. Syrup measured what is already free on disk: **95–100% of files have export symbols, 98–99% have some symbol, 100% have `lineCount`**, with real discriminators in them. `TN-N7`'s columns are the cheap half of this; content is the expensive half. ⚠️ **Syrup asks to review the row schema before I harvest at scale** — they have measured three models against three populations and can say whether a schema survives the coverage check, which is far cheaper than finding out after the LLM spend. **Accepted; schema goes to Syrup first.** | Syrup's groundwork note §§ 1, 12 · `ADR-2026-09-17-nutella-…` | **Schema DESIGNED 2026-09-17 in the ADR** — three layers, with content left as a future fourth layer so adding it costs no re-harvest. Still **PENDING** on two counts: the lead has not accepted the ADR, and **Syrup reviews the row schema before any harvest at scale** (accepted; not yet sent). | PENDING | Nutella (Team Nolan) |
| TN-N12 | **Pick harvest repos BY CLASS NEED, not by count — and treat 30 rows/class as a floor, 50 as a target.** Syrup's measured resolution rates show the algorithmic pass is a convention matcher and fires wildly differently per ecosystem: commerce 76.6%, got 75.8%, express 64.6%, n-dx 62.7%, Vue core 22.4%, svelte 14.7%, fastify 7.7%, **typeorm 5.7%** — so the *residue* that becomes corpus rows varies ~13× across repos. **"15-ish random public repos" reproduces the class skew**; random sampling is how seven ecosystems still produced one `hook` row. Class→ecosystem map: `hook`/`component`/`page`/`route-module` → a React/Remix/Next app (**nest, payload, remix already cloned**); `store` → a state-management lib; `model`/`schema` → an ORM (**typeorm already staged**); `middleware`/`route-handler` → express/koa/fastify; `cli-command` → any CLI. Recall floor from our own data: `entrypoint` at 59 rows → 85% recall; `types` at 34 → 42%; 1–2 rows → **0%**. ⚠️ Also: **the archetype catalog is moving under us** — Knight (`TJ-A3`) is adding an `algorithm` archetype and three `entrypoint` signals, measured at AsterMind unclassified 63.8% → 31.5%, on **no remote** (local commit `cb7f30de`). **Design the schema so a relabel is a script, not a re-harvest.** | Syrup's groundwork note §§ 6, 7, 8 | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N4 | **The builder harvests the RESIDUE, not the repo — the mission as stated is not what the tooling does.** `elm-corpus-build.mjs` takes `source: "llm"` rows, which is what is left *after the rules run*; a confidently rule-classified file never reaches the teacher and never enters the corpus (`elm-corpus-build.mjs:24-33` documents the trade, but not its effect on class coverage). **This is why `page` has 0 rows and why more repos will not fix it** — n-dx has **37 `page` files, all rule-caught, 0 reaching the teacher**, independently confirmed by the sanity corpus holding exactly 37 `page` rows. 293 n-dx files sit in thin classes, all rule-labelled, none in the corpus. **Three options, all lead's calls:** (1) `--source=algorithmic` — free, but a model learns the regex; (2) label everything — ~3× cost, via a fork of `scripts/elm-token-baseline.mjs:112` (Butter already replicated the module-private prompt), so **no edit to Jam's or Jarrett's files**; (3) accept residue-only and state it in `ELM-CORPUS.md`. Measured on n-dx only. | Jam's handover note § 2 | **DECIDED 2026-09-16 by the lead: SHIP TWO DATASETS.** Residue-only (LLM-labelled) and rule-labelled ship as separate artifacts with separate warranties, **never merged** — a row whose label means "a teacher judged" *or* "a regex fired", with nothing recording which, is `TN-J31`'s defect again. Between them they cover all 17 archetypes, and it costs **zero additional LLM calls** because `elm-archetype-corpus-sanity.json` already exists (`TN-N5`). Consumer picks, and each dataset's document says what its labels actually mean. | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N5 | **A third corpus artifact exists and is in nobody's plan: `scripts/data/elm-archetype-corpus-sanity.json`.** Committed 2026-08-13, unused since. Verified: **473 rows, 12 classes, `sources: ["algorithmic"]`**, n-dx 428 + AsterMind-CE 45 — `utility` 23.0%, `component` 15.2%, `cli-command` 13.3%, `entrypoint` 11.2%, `store` 11.0%, **`page` 7.8%**, `hook` 5.9%. **It is the inverse population — every class v2 starves of, it has.** ⚠️ **Do not merge it into v2:** a corpus where a label means "a teacher judged" or "a regex fired" depending on the row, with nothing recording which, is `TN-J31`'s two-unrecorded-teachers defect again. **If both ship, they ship as two datasets with two warranties.** | Jam's handover note § 3 | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N6 | **Structural coverage ceiling — `ELM-CORPUS.md` must state it before a consumer finds it.** Verified on `.sourcevision/inventory.json`: **1,525 entries — test 825 · source 683 · build 10 · config 6 · docs 1.** Classification only ever sees `role: "source"`, so **825 test files (54% of the repo) never reach either model — while `test-helper` is one of the 17 archetypes this team is chartered to cover.** v2's 29 `test-helper` rows come from other repos' helpers that landed on the source side of the role split. Ships alongside the other warranty items: residue-only harvest, and the `[partial signals]` teacher contamination (`TN-J26`) — the teacher is **not** shown the path only, so these labels are a strong model agreeing/disagreeing with a weak rule guess it was shown, **not an independent judgement**. | Jam's handover note §§ 4, 6 · Syrup's groundwork note | **DONE 2026-09-16 — `ELM-CORPUS.md` substantially amended**, with an index of the seven changes at the top because two of them retract things the document previously asserted. Landed: § 7 the 105-row retraction **at source** (it had already propagated into two notes — Syrup quoted it from here hours after I retracted it elsewhere, which is exactly why the fix had to be here); § 4 the teacher-recording correction; §§ 1a/5a the residue mechanism with the per-class rule-vs-LLM table; § 5b the sanity corpus and the two-datasets decision; § 3a the `[partial signals]` teacher caveat plus the retry ladder, batch composition and abstention; § 6 the coverage-check OOM and the unsourced 28.0%; § 10a the `ELM.train()` defect with its blast radius. | DONE | Nutella (Team Nolan) |
| TN-N7 | **Should a row carry the free measured columns, or the path string alone?** `sourcevision` computes `imports.json`, `inventory.json`, `zones.json`, `callgraph.json` **before** classification, and `enrichClassificationsWithLLM(classifications, inventory, imports)` **references neither in its body** (verified: one occurrence each, the parameter list). `inDegree`/`outDegree`/`role`/`language`/`loc`/`zone` cost nothing and **transfer across repos in a way path tokens cannot** — "imported by 30 files" means the same in hono as in n-dx; the token `rex` means nothing outside this repo. Jam's mechanical reading of v1's collapse: TF-IDF vocabulary is fitted on the training split, so a fresh repo's tokens get no slot, the vector goes sparse, and the model falls back to the 64% `service`/`utility` prior — matching the observed 96.4%. **Tension to resolve with the lead:** the steer is *don't let the consumer design influence the training data*, which forbids curating rows to flatter a gate; recording more of what was already measured is arguably a different thing, and **shipping the path alone is itself a design choice that constrains every consumer to the feature set that already failed once.** | Jam's handover note § 7 | **DECIDED 2026-09-16 by the lead: ADD THE FREE COLUMNS — and IMPLEMENTED.** `elm-corpus-build.mjs` now joins `inventory.json`, `imports.json` and `zones.json` by path and emits `role`, `language`, `loc`, `inDegree`, `outDegree`, `zone` on every row. **Verified: 255/255 n-dx rows carry all six.** Columns are per-repo optional and never fabricated — a repo analyzed without the import graph contributes rows with no degree fields, because a real `inDegree: 0` (nothing imports this file) must not be confused with "not measured". Provenance gains `rowColumns` (the union actually observed), `featuresAvailable` per repo, and `bySource`. **Deliberately NOT a bumped `schema` string** — the corpus generations are already called v1/v2 and a second unrelated "v2" is the exact ambiguity `ELM-CORPUS.md` § 2 warns about. ⚠️ **Existing committed artifacts are untouched**; the richer rows appear on the next rebuild. | DONE | Nutella (Team Nolan) |
| TN-N8 | **Three per-row variables vary and are recorded nowhere** — same shape as the teacher-model gap, and all inside corpus acquisition. **(a) The retry ladder silently swaps the prompt:** `computeLLMClassifyAttempts` (`classify.ts:394-396`) is `full → compact (archetype descriptions dropped) → compact + batch halved to 15`, and `promptLevel` is only `console.log`'d at `:418-420` — **never persisted**, so an unknown share of rows were labelled by a materially weaker prompt. **(b) Batch composition is uncontrolled:** `LLM_BATCH_SIZE = 30` (`:322`), all 30 paths share one context, so re-running the build at a different file ordering may not reproduce the same labels — **a reproducibility property for a dataset**, never measured. **(c) Abstention is invited and uncounted:** the prompt ends `"Omit files with no clear fit"` (`:509`); Vue core returned **23 of 303 unclassified**, visible only as a smaller `harvested` count. The gold-set script records `omittedByLlm`; the corpus builder discards it. Fix all three in the builder at the same time as the teacher-model rebuild. **(c) LANDED 2026-09-16** — `omissionCount()` now records it per repo. ⚠️ **The obvious implementation is a fabricated zero and I wrote it first:** an LLM-omitted file is not recorded as `source: "llm"`, it is left exactly as the algorithmic pass left it (`{archetype: null, confidence: 0, source: "algorithmic"}`) and there is no `llmAttempted` flag, so counting `source === "llm" && !archetype` returns **0 on every repo ever analyzed**. Caught by checking against Vue core, where 23 omissions were already known. The honest derivation: once the LLM pass has run, whatever is still unclassified is what it declined — and it returns **`null`, never 0**, when the pass did not run, because "not measured" and "zero omissions" must not collapse. **Verified: core → 23 (matches Jam's independent number), svelte → `null` (rules-only, 331 unclassified).** Caveat recorded in the code: this cannot separate a file the teacher declined from one a failed batch never reached — that needs (a). **(a) `promptLevel` and (b) batch composition remain open.** | Jam's handover note § 5 | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N1 | **Announce Team Nolan's database scope to the team.** The leads assigned scopes 2026-09-16: Team Nolan builds the ELM's training database (the green box of the lead's diagram — stage repos, label with the 17 archetypes via the `sourcevision` classify pass, ship a corpus). Written as a within-team note so a revived agent picks it up. Carries three verified corrections: the corpus-v2 re-freeze is **finished** (`IN-FLIGHT.md` § 2 still says `pid 25842` is RUNNING; `elm-frozen-model-v2.json` exists, 12,933 bytes), `elm-corpus-build.mjs` **does** record the teacher model as of `b4fde7b2` (`ELM-CORPUS.md` § 4 says it does not — so `TN-J31` needs a rebuild, not code), and the § 4 teacher table is recovered, not recorded. | [`NOTE-nolan-internal-2026-09-16-nutella-scope-and-database-mission.md`](Notes/NOTE-nolan-internal-2026-09-16-nutella-scope-and-database-mission.md) | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N2 | **Deliver the shareable training database — the standing mission.** A GitHub repo or comparably light database the other two leads can train an ELM from: rows, the seeded split, provenance including the resolved teacher per repo, and the warning label (`ELM-CORPUS.md` §§ 6-7) that makes the data safe to use. **Not a model.** Known gaps to close: `page` has **zero** rows and 8 classes are under 10 (`hook` at 1), so the corpus covers 16 of the catalog's 17 archetypes; ~~105 already-paid-for rows from gold set #2's unsampled candidates are unharvested~~ **🔴 RETRACTED 2026-09-16 — do not harvest them.** Verified from `k2-goldset2-llm-labels.json` (`poolSize: 355`, `sampled: 250`, provenance = **hono + trpc only**): the 105 are files from our **only two fresh-ecosystem generalisation probes**. Harvesting puts their paths in the training vocabulary and turns the one probe we own into a held-out test on a trained-on ecosystem — the instrument that failed to detect v1's collapse. The existing contamination assertion is **path-level** and would **pass**; the contamination is **ecosystem-level**. Blocked behind replacement probes. Caught by Jam; `nest`/`payload`/`remix` are cloned but never analyzed and `svelte`/`typeorm` were analyzed rules-only (0 LLM rows — `--fast` gates the classify pass). ⚠️ **Blocked on a prerequisite that is not mine:** corpus v2 has never been coverage-checked, and v1's model collapsed on unseen repos (34.9% → 13.2% coverage). **Deliverable shape DECIDED 2026-09-16 by the lead: a separate GitHub repo** — corpus JSON + `ELM-CORPUS.md` + the builder, standalone, so the other two leads can consume it without a workspace dependency or `pnpm-lock.yaml` churn. ⚠️ **Creating a repo under the org is outward-facing and needs a second lead's sign-off** before it exists; harvesting and corpus work proceed meanwhile and are reversible. | [`ADR-2026-09-04-syrup-merge-elm-corpus-into-jarrett-harness.md`](../ADR/ADR-2026-09-04-syrup-merge-elm-corpus-into-jarrett-harness.md) · [`ELM-CORPUS.md`](ELM-CORPUS.md) | IN-PROGRESS | Nutella (Team Nolan) |
| TN-N3 | **Seam to settle with Jam: who owns `TN-J32` and `TN-J9` now that corpus acquisition is a separate scope.** `TN-J32` (corpus v2) and `TN-J9` (ecosystem diversity) are corpus-acquisition rows sitting on Jam's absorbed-from-K2 claim, and corpus acquisition is now Nutella's scope. **Not claimed unilaterally** — the board is the lock and Jam's claim predates mine. Raised in § 3 of the `TN-N1` note; needs the lead. The concrete consequence: the free, label-free v2 coverage re-check (`elm-coverage-check.mjs`) gates whether v2 can be offered to anyone, and **it is unrun with nobody clearly on it.** | [`ELM-CORPUS.md`](ELM-CORPUS.md) § 6 | **ANSWERED 2026-09-16 by Jam, pending the lead's ratification:** `TN-J9` → Nutella (corpus acquisition by its content). `TN-J32` **splits** — the corpus-v2 rebuild → Nutella; the coverage re-check stays **Jam**, because it loads a frozen artifact, re-fits nine ELMs and scores predictions, i.e. it is a *model* evaluation, and Nutella should not be blocked on Jam's row for a gate on Nutella's own deliverable. `elm-archetype-corpus-sanity.json` → Nutella. ⚠️ Jam also warns the check **OOMs on the v2 model** (recipe not weights → re-fits nine 4096-unit models, dies ~1978 MB vs node's ~2096 MB default); `--max-old-space-size=6144` gets through, so **Step 0 is not the free five minutes the ADR implies**. | IN-PROGRESS | Jam + Nutella — lead to ratify |
| TN-S1 | **Survey Teams Jarrett and Thomas and report their findings to Jam and Butter.** Their work exists only on their own branches; nothing merges it into our view, so nobody on Team Nolan currently knows what either team has found. Read `origin/Jarrett`, `origin/Thomas_Branch`, `origin/Thomas's_Branch` end to end and write within-team notes stating what they found and what it changes for Path A (Butter) and Path B (Jam). **Read-only on their branches — reports, does not edit.** **DELIVERED 2026-08-31.** Two notes written. **Finding: all three teams independently built an ELM classifier into `packages/sourcevision/src/analyzers/` — Jarrett 362 lines + 28 tests (`classify.ts` untouched), Thomas 133 lines + 86 lines inside `enrichClassificationsWithLLM`, both widening `FileClassification.source` with `"elm"`, both shipped disabled by default.** Neither team's claim is on `IN-FLIGHT.md`. Jam's 08-11 zero-evidence finding is confirmed twice independently (Thomas 0/260, margins ~0.002; Jarrett 100% across 5 codebases). Both other teams then proposed raw path text as the fix — which is Butter's prototype, already built and measured. Also verified: `useTokenizer: true` joins tokens on the empty string (`astermind.umd.js:771-773`), so text mode has no token embeddings; `charToOneHot` lowercases, so Butter's uppercase charSet adds 26 dead slots; `TJ-R1` is DONE citing an ADR that exists on no branch; the `pnpm.overrides` break is pnpm 11, not 10.33. | _(reports on theirs; produced none)_ | DONE — open follow-up is Nolan's call on outbound notes | Syrup (Team Nolan) |
| TN-J1 | Survey the monorepo for LLM call sites replaceable by ELM/KELM to cut token spend; characterise each as classification-shaped vs open-ended, then propose a three-way split across Teams Nolan / Jarrett / Thomas. **Delivered:** 22 call sites triaged, only 2 replaceable; split proposed. | [`ADR-2026-08-11-jam-elm-replacement-survey-and-split.md`](../ADR/ADR-2026-08-11-jam-elm-replacement-survey-and-split.md) | DONE | Jam (Team Nolan) |
| TN-J2 | Get the split ADR accepted, amended, or rejected by the three leads. **Team Nolan has taken Path B** (2026-08-11); paths A and C still need Jarrett and Thomas. | [`ADR-2026-08-11-jam-elm-replacement-survey-and-split.md`](../ADR/ADR-2026-08-11-jam-elm-replacement-survey-and-split.md) | IN-PROGRESS | Nolan (Path B claimed; A/C open) |
| TN-J4 | **Build the ELM archetype classification tier.** Decision taken to proceed despite the modest measured prize — inference is local and free, so any hit is pure gain and the confidence gate bounds the downside. Kill criterion agreed up front: ≥30% of the residue at or above LLM accuracy, else publish the negative. | [`ADR-2026-08-13-jam-proceed-with-elm-classification.md`](../ADR/ADR-2026-08-13-jam-proceed-with-elm-classification.md) · [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) | PENDING | Nolan (Team Nolan) |
| TN-J5 | **Step 1 of TN-J4 — `archetypes.ts` gateway signal fix.** Anchor relaxed to `(?:^\|[-.])`; test written first and watched fail. Measured 424→428 classified, 259→255 unclassified, `gateway` 0→4. typecheck + 1192 unit + 108 e2e green. **Found: the other 5 zero-example archetypes are not fixable here** — their signals are correct for conventions n-dx doesn't use, so those classes must be populated from other repos. | [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) § Step 1 · commit `26a191e7` | DONE | Jam (Team Nolan) |
| TN-J7 | **Step 2 of TN-J4 — corpus acquisition. DONE.** `scripts/data/elm-archetype-corpus.json`, 324 LLM-labelled rows / 13 classes from n-dx + AsterMind-CE, seed 42, 241/83 split (commit `2e6a3e43`). Confirmed the premise — `service` 0→123, `middleware` 0→7 — but the majority baseline rose to **38.0%** and 9 of 13 classes are under 10 rows. | [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) § Step 2 · commit `2e6a3e43` | DONE | Jam (Team Nolan) |
| TN-J8 | ~~Blocker: no LLM reachable.~~ **RESOLVED 2026-08-20** — Nolan's Claude CLI was already on disk as the VS Code extension binary, just not on `PATH`. Nothing installed. Reached via `PATH` per-run, deliberately **not** persisted to `.n-dx.json` (committed + shared; path is machine- and extension-version-specific, so it would break Jarrett and Thomas). | — | DONE | Jam (Team Nolan) |
| TN-J11 | **Lane B (B1/B2) — calls-avoided instrument. DONE.** `scripts/elm-calls-avoided.mjs` + data (commit `f91370f8`), under Butter's measurement contract ADR. Measured: n-dx 9 batches/analyze, AsterMind-CE 3, total 12 (36 worst case with retries). Emits no token figure by design — that conversion is Path A's. | [`ADR-2026-08-23-butter-savings-measurement-contract.md`](../ADR/ADR-2026-08-23-butter-savings-measurement-contract.md) · [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § Lane B | DONE | Jam (Team Nolan) |
| TN-J13 | **`pnpm test` was red for the whole branch — my two ELM scripts failed `architecture-policy.test.js`. FIXED.** Both import `node:child_process` only for git provenance. Added to `ALLOWED` (commit `855dac54`) rather than routing through `@n-dx/llm-client`, which **does not resolve from `scripts/`** — it is not a root dependency, so route 1 would mean adding a package tier to the repo root or importing built `dist/` by path. Same rationale as the two existing `scripts/*.mjs` entries. Root suite `1996 passed / 1 skipped / 0 failed`. Found by Butter. | — · commits `855dac54`, `bcdfd9c9` | DONE | Jam (Team Nolan) |
| TN-J17 | ~~Constrain the agent harness on classification calls.~~ **CLOSED — NOT PURSUED (ADR status since 2026-08-23; this row was left `IN-PROGRESS` for twelve days and is corrected 2026-09-04).** Butter measured `num_turns = 1` on a real classify call, so there is no agent loop to constrain and the premise died. The ~95% overhead is per-spawn cost, not a harness we control. **Originally:** Butter's `A4` measured a real classify call at **53k–268k tokens with ~95% not prompt or completion**; I confirmed the mechanism structurally: `spawnOnce` (`cli-provider.ts:118-129`) passes only `-p --output-format --model`, and **three MCP servers are connected**, loading their tool schemas into every call for a task needing zero tools. `cliFlags` is already on the public `CompletionRequest` (`types.ts:76`) and **no call site anywhere sets it**, so the fix is one array in `claude-client.ts:145-149` with **zero `llm-client` changes**. ⚠️ **This shrinks Path B's own prize by ~an order of magnitude and may moot `TN-J4` Step 3** — raised deliberately as a lead decision. | [`ADR-2026-08-23-jam-constrain-cli-harness-for-classification.md`](../ADR/ADR-2026-08-23-jam-constrain-cli-harness-for-classification.md) · [`IMPL-…`](../IMPL/IMPL-2026-08-23-jam-constrain-cli-harness-for-classification.md) | DONE (closed — not pursued) | Jam (Team Nolan) |
| TN-J16 | **Path B's share of the analyze call budget is not a constant, and `TN-J12` needs a third axis.** Pass-1 budgets are now measured: n-dx **9 classify + 2 enrich = classify is 82%**; AsterMind-CE **3 + 2 = 60%**. But AsterMind-CE's full 4-pass first analyze was 9 calls total, putting classify near **33%**. **This refutes what I published earlier** — that n-dx's 26 zones vs 11 would make its classify share *smaller*; zone count does not become call count (`ZONES_PER_BATCH=7` plus a bypass that drops 46% of n-dx's zones means 26 zones cost 2 calls while 683 files cost 9). The share moves with repo shape **and** with how many enrichment passes run, and a **re-analyze resumes at pass N+1 on changed zones only**, collapsing enrichment while classify persists. So `TN-J12` must decide not just steppy-vs-averaged but **first-analyze vs re-analyze**. | [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § Lane B · commit `4353f40a` | PENDING | _(finding recorded — decision is `TN-J12`'s)_ |
| TN-J15 | **Sourcevision already avoids LLM calls deterministically — now measured, and it is worth less than the zone count suggests.** `isStructuralZone` (`enrich.ts:279-288`) templates zones with no `role: "source"` file, using **zero** LLM calls. Replicated exactly from `zones.json` + `inventory.json` (commit `4353f40a`): **n-dx 12 of 26 zones structural, AsterMind-CE 2 of 11.** **But the unit is calls, not zones** — enrichment batches at `ZONES_PER_BATCH=7`, so n-dx's bypass is worth **2 calls/pass** (4→2) and AsterMind-CE's is worth **ZERO** (2→2) despite removing 2 zones. **The bypass has exactly the lumpiness that made my own Step 1 worth nothing** — a pleasing confirmation that the unit of account in Butter's ADR is the right one. Validates Path B's thesis (deterministic call-avoidance is real and ships) while showing the residue it leaves is smaller than a zone-percentage framing implies. | — · commit `4353f40a` | DONE | Jam (Team Nolan) |
| TN-J14 | **⚠️ Classify is a strict subset of a full analyze's LLM calls — Path B's ceiling is a minority of analyze spend.** Butter's instrumented `--full` run on AsterMind-CE recorded **9 total calls**; 69 files were LLM-labelled, so classify made **at least** `ceil(69/30) = 3`. The rest are zone enrichment (`enrich-batch.ts:70,217`, `enrich-per-zone.ts:159`), gated by the same `!fastMode` flag as classify (`analyze-phases.ts:219` vs `:277`) but generating prose — **not ELM-replaceable**. **Correction, same day:** an earlier revision of this row asserted a **3 + 6 / 33%** split as measured. It is consistent with the code but is *inference* — `manifest.tokenUsage` is a single aggregate with **no per-phase breakdown**, so the 9 cannot be decomposed from any artifact. What stands is the direction, not the percentage. **The split also can't be computed statically:** enrichment = Σ passes `ceil(|changed non-structural zones| / 7)`, shrunk by two data-dependent reducers — the structural-zone bypass (build/asset/doc/config-only zones templated with **zero** LLM calls, `enrich.ts:133`) and per-zone content-hash filtering (passes 2+ enrich only changed zones, `enrich.ts:152`). **n-dx's denominator therefore needs either a paid full analyze or per-phase call attribution in the manifest** — the latter proposed to Butter as it sits in the token-accounting lane. Feeds `TN-J12`. | [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § Lane B · commits `bcdfd9c9`, `42689ed9` | PENDING | _(finding recorded — publishing decision is `TN-J12`'s)_ |
| TN-J12 | **Calls-avoided is lumpy, and Step 1 avoided zero calls.** Batches are `ceil(files/30)`, so file-level wins only pay when they cross a boundary: the gateway fix reclassified 4 real files, 259→255, and stayed at 9 batches. Thresholds before the *first* call is avoided: n-dx **15 files (5.9%)**, AsterMind-CE **9 files (13.0%)**. Decide what we publish — per-analyze (steppy, what users feel) or averaged. | [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § Lane B | PENDING | _(unclaimed — needs the leads)_ |
| TN-J23 | **Implement the ELM tier — ADR + IMPL up (lead's decision to proceed).** Plan: retune (capacity sweep to plateau, architecture sweep, `ridgeLambda`) selected by **train-CV only**; re-certify on a **fresh gold set**; integrate behind a **never-worse gate**; dark-run before enabling. ⚠️ **Gold set #1 is spent** — I have read its labels, so it is now a dev set and its numbers may never be published as results. **Certification requires a second gold set; without one the tier cannot ship** — not because the model is bad but because we would have no honest way to say. Ship criteria S1/S2/S3 fixed in advance. **Not blocked by Butter's lockfile issue** — everything runs on the root-resolvable dependency. | [`ADR-2026-08-28-jam-implement-the-elm-tier.md`](../ADR/ADR-2026-08-28-jam-implement-the-elm-tier.md) · [`IMPL-…`](../IMPL/IMPL-2026-08-28-jam-elm-tier-implementation.md) | IN-PROGRESS | Jam (Team Nolan) |
| TN-J25 | **K1 replaced — ADR up, needs the leads.** K1 (`≥3 of 9 calls on n-dx`) is broken for three arithmetic reasons: it is **non-monotone** (better rules shrink the residue, so 255→180 files raises the bar from 29.4% to **50%** coverage), it is **unreachable on small repos** (3 calls on AsterMind-CE = 100% coverage), and it is **blind across a 30-file band**. Proposed **K1′: ≥30% coverage at certified precision** — a re-expression, not a relaxation, since 30% reproduces 3-of-9 on n-dx and 1-of-3 on AsterMind-CE while being a property of the model. Paired with **K2′**: compare against the LLM **on the files the tier claims**, which is what S1 already says in words and what `ADR § 3a`'s adoption evidence did not do. ⚠️ **The denominator is not cosmetic** — under the old model, 12 of 18 operating points beat the global 72.3% and **0 of 18** beat the LLM on their own files. | [`ADR-2026-08-28-k2-replace-k1-with-a-coverage-criterion.md`](../ADR/ADR-2026-08-28-k2-replace-k1-with-a-coverage-criterion.md) | IN-PROGRESS | Jam (Team Nolan) — absorbed from K2 2026-09-04 |
| TN-J27 | **Phase 1 closed — architecture IS a lever, and it was the one nobody swept.** `activation` was never tested; `relu` was the feasibility screen's default and every later script inherited it, exactly as `hiddenUnits: 256` had been. **`tanh` scores 64.6% vs relu's 60.6% — +4.0 pp on 9 of 9 paired runs** (train-CV only, gold untouched, bar pre-registered at `f4a06175`). `gelu` ties (64.59 vs 64.64) and is an equivalent fallback. Linear `KernelELM` also clears (+3.0 pp, 7 of 9). **Not levers:** `ridgeLambda` is flat from 1e-4 to 1e-1 — *the sweep the plan had the highest prior on* — stacked voting **loses** 2.8 pp, and RBF loses at every γ. **Two of two untested defaults have now been worth ≥4 pp each.** ⚠️ Capacity was measured entirely on `relu`; the capacity × activation interaction is untested and needs its own pre-registration. | [`IMPL-2026-08-28-…`](../IMPL/IMPL-2026-08-28-jam-elm-tier-implementation.md) § Phase 1 RESULT · `scripts/elm-architecture-sweep.mjs` | DONE | K2 (Team Nolan) |
| TN-J28 | **⚠️ I nearly published K2's error a second time.** My first Phase 2 run used `relu` — a model Phase 1 had **already rejected** — and returned **0 of 18 designs beating the LLM**, together with a drafted ADR recommending the tier not ship. Re-run on the adopted `tanh` model: **5 of 18 beat**, and `B+su` at **32.9% coverage / 75.1% vs 74.1% / 3 of 9 calls** meets both bars in the mean. **This is exactly the `hiddenUnits: 256` mistake, one iteration later.** What caught it was not judgement but the pre-registered rule that *Phase 2 runs the model Phase 1 adopted*. Recorded because the guard, not the insight, is the transferable part. | [`IMPL-…`](../IMPL/IMPL-2026-08-28-jam-elm-tier-implementation.md) § Phase 2 RESULT | DONE | K2 (Team Nolan) |
| TN-J26 | **The teacher and the student are not shown the same thing.** The handbook's map says the classify prompt is **path only**. It is not: `classify.ts:499-503` appends `[partial signals: service(0.7), …]` from the algorithmic pass, and `evidence` is populated whenever **any** signal matches (`:159`) — independent of `PRIMARY_THRESHOLD`, which is what makes a file unclassified in the first place. So residue files carry archetype hints into the teacher's prompt while **the ELM gets the path string and nothing else.** ⚠️ **Unquantified, deliberately** — `.sourcevision/classifications.json` overwrites the algorithmic `evidence` with the LLM's returned reason, so the pre-LLM state is unrecoverable from any committed artifact and needs a corpus rebuild. **This is not the rejected feature work** (that was re-encoding the same path, −20.1 pp); it is a *different signal*, and the point is that we have been scoring a student against a teacher with a larger question paper. | [`ADR-2026-08-28-k2-…`](../ADR/ADR-2026-08-28-k2-replace-k1-with-a-coverage-criterion.md) § 8 | PENDING | _(unclaimed)_ |
| TN-J29 | **Correction to "the LLM's label noise is random".** The `service`/`utility` axis is symmetric (6 vs 5) and that reading is right — but it is the **only axis that was examined**. On the 24 held-out files whose truth is *not* `service`/`utility`, the LLM makes 7 errors and **6 of them collapse a minority class into `service`/`utility`** (`cli-command`, `config`, `entrypoint`, `gateway`, `types` → `utility`/`service`; error flow into `utility` 11, out 8). **`utility` is the teacher's sink for uncertainty** — directional and systematic, which is precisely what a student cannot average out. It also explains abstention's coverage shortfall without appeal to model weakness. | [`ADR-2026-08-28-k2-…`](../ADR/ADR-2026-08-28-k2-replace-k1-with-a-coverage-criterion.md) § 5 | DONE | K2 (Team Nolan) |
| TN-J31 | **The corpus does not record which model labelled it — and it was not one model.** `n-dx` carries `.n-dx.json` with `llm.claude.model = claude-sonnet-4-6` (**255 rows**); `AsterMind-CE` has no `.n-dx.json`, so it resolved `NEWEST_MODELS.claude = claude-sonnet-5` (**69 rows**). That constant has been `sonnet-5` since 2026-07-01, *before* the corpus was built, so this is not drift — **corpus #1 has always been 79% one teacher and 21% another**, and `elm-archetype-corpus.json`'s provenance block records neither. Does not invalidate the corpus, but `elm-corpus-build.mjs` should capture the resolved model per repo, and every future 'LLM vs truth' figure should name its teacher. Found while pinning the teacher for gold set #2. **PARTIALLY CLOSED 2026-09-04 — `elm-corpus-build.mjs` now resolves the teacher per repo** (target's `.n-dx.json` → `llm.claude.model`, else `NEWEST_MODELS.claude` parsed from `config.ts` so it cannot drift), records it per repo plus a row-weighted mix in provenance, and **prints a MIXED-TEACHER warning at build time** — this went undetected for 19 days because nothing said it out loud. ⚠️ **Corpus v2 carries the same mix and its artifact still does not record it:** 255 rows `claude-sonnet-4-6` (n-dx-1) / 369 rows `claude-sonnet-5` (the other six). That composition is verified and documented in [`ELM-CORPUS.md`](../Nolan-Agents/ELM-CORPUS.md) § 4 **as recovered, not recorded** — deliberately not backfilled into the JSON, because a hand-edited provenance block would look like build-time evidence. Rebuilding v2 with the fixed script records it properly at a cost of 0 LLM calls. | [`IMPL-2026-08-28-…`](../IMPL/IMPL-2026-08-28-jam-elm-tier-implementation.md) § Phase 3 amendment · `scripts/elm-corpus-build.mjs` | PARTIALLY CLOSED | Jam (Team Nolan) — absorbed from K2 2026-09-04 |
| TN-J30 | **Gold set #2 built — and the tier fails K1′ before a single file was labelled.** Funded by the lead 2026-08-31, Option B. Model **frozen first** (`elm-frozen-model.json`, hash `d794f847…`, 9-seed vote); protocol and the certification script both committed **before** labels existed. Spent **12 classify calls** (hono 4, trpc 8) on two fresh repos, sampled 250 files, contamination check passes mechanically (0 of 355 candidates in corpus #1). **Then coverage — which needs no ground truth — came back at 13.2% against K1′'s 30%.** Packet is built and blind, ready to label if the lead still wants the quality answer, but at 13.2% coverage 250 files yields only ~33 claimed — short of the ~83 that Option B's 5 pp margin requires. | [`IMPL-2026-08-28-…`](../IMPL/IMPL-2026-08-28-jam-elm-tier-implementation.md) § Phase 3 · `scripts/elm-{freeze-model,goldset2-packet,coverage-check,certify}.mjs` | BLOCKED (lead decision) | Jam (Team Nolan) — absorbed from K2 2026-09-04 |
| TN-J32 | **⚠️ The tier does not generalise. It learned n-dx's archetype prior, not a path→archetype mapping.** Measured with no ground truth, so it cost nothing: on the ecosystems it was trained on the model's class prior tracks the teacher **exactly** (ELM says `service`/`utility` 72.3%, teacher 72.3%); on two fresh repos it **collapses onto the majority class** — ELM 96.4% vs teacher 48.4%, **241 of 250 files** predicted `service`/`utility`, **5 distinct labels used against the teacher's 13**. Coverage falls 34.9% → 13.2%. Two of the packet's classes (`component`, `model`, 18 files) have **zero** training rows, so the model cannot emit them at all. **This is `TN-J9` ('the corpus needs ecosystem diversity, not more repos') coming true, filed 2026-08-13 and never claimed.** A fix exists and is cheap: 105 of the 355 fresh LLM-labelled files were **not** sampled into the packet, so they can extend the training corpus while the 250 stay clean as a gold set. That is a new Phase 1, and it requires deliberately unfreezing. **UPDATE 2026-09-04 — the unfreeze was approved and pre-registered (`90e5bdb7`), corpus v2 is built (`1a5403c6`: 624 rows / 7 ecosystems, `service`+`utility` 73.8% → 63.6%, 13 classify calls), and Phase 1b HAS RUN on the declared grid: **`elm-4096 tanh` adopted, +2.06 pp on 7 of 9 paired wins** (train-CV only, fold seeds 7/13/29 × model seeds 101/202/303). `tanh` beats `relu` at equal capacity for the third time, so Phase 1's activation finding survives the corpus change. ⚠️ **4096 is the top edge of the declared grid and it won there** — no evidence the new plateau is reached; extending to 8192 is a separate pre-registration, approved by the lead 2026-09-04 and not yet run. **The generalisation question is still OPEN:** the coverage re-check against gold set #2 has not been run on the v2 model, so corpus v2 is an unvalidated fix. Corpus now documented as a cross-team asset in [`ELM-CORPUS.md`](../Nolan-Agents/ELM-CORPUS.md).** | `scripts/elm-coverage-check.mjs` · `scripts/data/elm-architecture-sweep-v2.json` · [`ELM-CORPUS.md`](../Nolan-Agents/ELM-CORPUS.md) | IN-PROGRESS | Jam (Team Nolan) — absorbed from K2 2026-09-04 |
| TN-J24 | **RESOLVED 2026-08-28 — merge the decision, not the taxonomy.** Three designs measured. **(A) merge in the product:** +26.8 pp, **rejected** — the merged class holds **74% of all files**, and a category covering three-quarters of a codebase carries almost no information; it buys accuracy by deleting the question. **(C) hierarchical** (12-class then a `service`/`utility` specialist): specialist tops out at **67.8%** vs a 51.4% baseline, giving **≈63.7% end-to-end — no better than the flat 64.1%.** **(B) abstention — ADOPTED:** the ELM answers only when it predicts a non-`service`/`utility` class, claiming **22.9% of files at 75.5% precision vs truth** against the LLM's 72.3%. ⚠️ **Satisfies S1 but FAILS S2** (2 of 9 calls, needs ≥3), and these are DEV numbers whose range (68.2–81.3%) straddles the bar. **Also corrected: I had overstated the blast radius — no production code anywhere branches on these ids** (only `archetypes.ts` defines them; the other 44 references are tests). **AMENDED 2026-08-28 by `TN-J25`: the shape was right, the arithmetic behind the headline was not.** The 75.5%-vs-72.3% comparison put a hand-picked subset against the LLM's **global** average, which is not what `ADR § 5`'s own S1 asks for. Measured on the same files, and on the model Phase 1 adopted, abstention beats the LLM by **+1.8 pp** (74.7% vs 72.9%) — a real result, reached by different arithmetic. Its coverage ceiling is also now known to be **structural**: 26.2% of the corpus is non-`service`/`utility`, so abstention alone can never reach K1's 29.4%, at any precision, with any model. | [`ADR-2026-08-28-jam-implement-the-elm-tier.md`](../ADR/ADR-2026-08-28-jam-implement-the-elm-tier.md) § 3a · [`ADR-2026-08-28-k2-…`](../ADR/ADR-2026-08-28-k2-replace-k1-with-a-coverage-criterion.md) § 3–4 | AMENDED | Jam (Team Nolan) · K2 (amendment) |
| TN-J21 | ~~Path B should not ship.~~ **AMENDED 2026-08-28 — my recommendation rested on incomplete evidence, and the lead's decision to continue is supported.** The K2 model used `hiddenUnits: 256`, a value I picked arbitrarily writing the feasibility screen and **never tuned**. Diagnostics (train-CV only, gold untouched): capacity 256→1024 is worth **+12 pp and had not plateaued**; merging `service`/`utility` is worth **+26.8 pp**. **So K2 was a verdict on that model, not on the approach** — "the ELM is just worse" was not established and I over-generalised. The row is amended, not deleted: the sequence matters, and the untuned-model caveat is the point. | [`ADR-2026-08-28-jam-implement-the-elm-tier.md`](../ADR/ADR-2026-08-28-jam-implement-the-elm-tier.md) | AMENDED | Jam (Team Nolan) |
| TN-J22 | **The path-only premise was fine; my doubt about it was wrong — and the LLM is the one underperforming.** I hypothesised a low path-information ceiling would explain everything. **It is 85.4%** — a human reading only file paths reproduces their own content-informed judgement 85.4% of the time, so **paths are highly informative and the corpus is not built on sand.** The corollary is a finding for the whole project, not just Path B: **the LLM scores 72.3% against truth, 13.1 pp below what the path alone supports.** Improving the classify prompt is a cheaper, larger win than any ELM was going to be, and it is currently nobody's scope. | `scripts/data/k2-analysis.json` | PENDING | _(unclaimed)_ |
| TN-J20 | **K2 gold set — RETURNED AND ANALYSED. K2 FAILS.** 83 files labelled blind by Nolan, two passes, protocol followed (`585a1221`); packet integrity verified — immutable columns and row order unchanged, vocabulary valid, distributions match. **Results:** path-information ceiling **85.4%** · LLM vs truth **72.3%** · **ELM vs truth 54.4%** (range 47.0–59.0%, 7 seeds) · ELM vs LLM 59.9%. **K2 requires (3) ≥ (2); 54.4% < 72.3%, so it fails by 17.9 pp.** | [`ADR-2026-08-27-jam-k2-gold-set.md`](../ADR/ADR-2026-08-27-jam-k2-gold-set.md) · `scripts/elm-k2-analysis.mjs` | DONE | Jam (Team Nolan) |
| TN-J19 | **Confidence-gated deployment + a kill criterion that can actually be measured. ADR + IMPL up, needs the leads.** The gate works — top-decile precision **84.5%** vs 59.9% ungated — and must be **percentile-based** (13-class softmax caps confidence at 0.245, so absolute thresholds select nothing). At 30% coverage: **75.3% precision, 3 of 9 n-dx calls avoided.** ⚠️ **Retires my own 2026-08-13 kill criterion as unmeasurable** — "at or above LLM accuracy" is circular, since the LLM *is* the teacher that produced every label we own. Replaced by K1 (≥3 of 9 calls, arithmetic) and K2 (gated predictions beat the LLM on a **bounded `service`-vs-`utility` gold set of ~60 files** — the 74% of mass where both teacher and model are weakest). **That scoped gold set is my proposed answer to `TN-J10`.** | [`ADR-2026-08-27-jam-confidence-gated-deployment-and-a-measurable-kill-criterion.md`](../ADR/ADR-2026-08-27-jam-confidence-gated-deployment-and-a-measurable-kill-criterion.md) · [`IMPL-…`](../IMPL/IMPL-2026-08-27-jam-confidence-gated-classification-tier.md) | IN-PROGRESS | Jam (Team Nolan) |
| TN-J18 | **Step 3 feasibility screen — RUN. The mapping is learnable; Path B is not dead.** Bar pre-registered at `f3205143` **before** any model ran. Result on the 83 held-out rows, 7 seeds, path-string TF-IDF fitted on train only: **mean agreement-with-teacher 59.9%, range 51.8–67.5%, vs a 37.3% recomputed majority baseline (+22.5 pp).** **Verdict: PROCEED** — and critically **0 of 7 runs fell below the 45% not-viable threshold**, so infeasibility is excluded, not merely unproven. 1 of 7 landed in the inconclusive band, so a single-run claim would be unreliable — quote the mean with the range. ⚠️ Two near-misses recorded in the script as guards: `trainFromData` accepts **string labels silently and trains nothing** (first run gave 2.4% and would have been published as "not viable"), and `cfg.seed` **defaults to 1337** so unseeded repeats are identical copies (first fix gave a fake "range 57.8–57.8%"). | [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) § Step 3 · `scripts/elm-feasibility-screen.mjs` | DONE | Jam (Team Nolan) |
| TN-J10 | ~~Does Step 3 need a hand-labelled gold set?~~ **RESOLVED 2026-08-27 by `TN-J20`.** Yes, and it was decisive: the gold set is what turned a 59.9% agreement figure into a **verdict**. Against truth the LLM scores 72.3% and the ELM 54.4%, so what looked like "the ELM inherits the teacher's fuzziness" was mostly **the ELM being worse**. Blocked Step 3 from 2026-08-20 to 2026-08-27. | [`ADR-2026-08-27-jam-k2-gold-set.md`](../ADR/ADR-2026-08-27-jam-k2-gold-set.md) | DONE | Nolan (labeller) · Jam (analysis) |
| TN-J9 | **⚠️ CONFIRMED 2026-09-01 by `TN-J32` — this was right, and it is now the blocker.** The tier trained on n-dx + AsterMind-CE collapses to the majority class on two fresh repos (coverage 34.9% → 13.2%), and `component`/`model` have zero training rows so it cannot emit them at all. Filed 2026-08-13, never claimed, and it is exactly what stopped Phase 3. **Corpus needs ecosystem diversity, not more repos.** n-dx + AsterMind-CE added 473 rows but zero new classes; `middleware`, `model`, `route-module`, `service`, `test-helper` are still at zero. Need a NestJS/Angular app, an Express server, an ORM-backed API, a Remix app, and any repo using `__mocks__`/`fixtures`. | [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) § Step 2 | PENDING | _(unclaimed)_ |
| TN-J6 | **Should shipping `archetypes.ts` changes invalidate the classification cache?** Users who upgrade and receive better rules see no benefit until they run `--full` (`analyze-phases.ts:210`). Options: a ruleset version stamp that forces re-classification, release-note guidance, or accept staleness. Affects every future archetype change, not just the ELM work. | [`IMPL-2026-08-13-jam-elm-classification-build.md`](../IMPL/IMPL-2026-08-13-jam-elm-classification-build.md) § Open questions | PENDING | _(unclaimed)_ |
| TN-F1 | **The documented branch convention has never been used.** Four docs (`GITHUB-WORKFLOW.md` § 2, `OWNERSHIP.md` § Naming, `claude-context-instruction` § 4, `Command-Structure` § Rules) specify `elm/<lead>/<topic>` → `origin/main`; reality is `<TeamName>` → `dev` → `main`, and no `elm/*` branch has ever existed on any remote. Worse, **`origin/main` carries no `Claude-Context/` at all**, so `NEW-AGENT.md`'s worktree step produces a worktree with no agent system in it. Reconcile all four docs in one pass. Needs the three leads — a rule binding every team is a collective decision, so ADR first, edit after. **ADR submitted; the doc edits wait on the leads.** | [`ADR-2026-08-11-fluff-branch-and-base-conventions.md`](../ADR/ADR-2026-08-11-fluff-branch-and-base-conventions.md) | IN-PROGRESS | Fluff (Team Nolan) |
| TN-F2 | **Note filenames addressed agent-to-lead instead of lead-to-lead.** Lead's call, 2026-08-11: `NOTE-<from-lead>-to-<to-lead>-…`, intern names only, drafting agent named in the body. Renamed all four Team Nolan outbound notes, updated the convention in `Command-Structure`, `claude-context-instruction`, `OWNERSHIP.md`, and all three `Notes/README.md`, and notified Teams Jarrett and Thomas. Within-team form `NOTE-<lead>-internal-…` was my call, flagged for override. | — (lead's directive, no ADR) | DONE | Fluff (Team Nolan) |
| TN-F3 | **Notes were written but never delivered, and nothing at startup looked for them.** Six Team Nolan notes existed only on `Nolan-Work` — `origin/Jarrett` had one from Aug 5, `Thomas_Branch` had none. Separately, neither auto-loaded file (`CLAUDE.md`, `AGENTS.md`) references `Claude-Context` at all, so an agent only reads its inbox if a human pastes the prompt telling it to. Fixed the delivery half by merging `Nolan-Work` → `dev` (`d1967288`); added `Claude-Context/hooks/unread-notes.sh` as a **local-only, never-committed-wiring** inbox reader. Jarrett and Thomas must still merge `dev` into their branches. | — (lead's directive) | DONE | Fluff (Team Nolan) |
| TN-J3 | **Token accounting read zero — ROOT-CAUSED AND FIXED.** All 6 `.hench/runs/*.json` do record `tokenUsage {"input":0,"output":0}`, and the parsers are wired in (`cli-provider.ts:37` imports them; called at `:348`, `:357`, `:378`). **Correction (Butter, 2026-08-23): all six runs are dated `2026-02-04`, and `event-accumulator.ts` — which does the accumulating — was added `2026-04-21` in `0269cf75`.** The evidence predates the mechanism by 2.5 months, so it does not establish a live defect; it establishes that nobody has measured since February. First step is a fresh run, not a fix. Also: `.hench/runs/` is gitignored (`.gitignore:5`) yet those six are tracked, so new runs will not commit and fresh evidence needs a deliberate fixture. Until a number exists, Paths B and C cannot report a saving. | [`ADR-2026-08-23-butter-savings-measurement-contract.md`](../ADR/ADR-2026-08-23-butter-savings-measurement-contract.md) · [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § Lane A **RESULT (Butter, 2026-08-23): it was a live defect after all.** Measured a completed `analyze --full`: `{calls: 9, inputTokens: 0, outputTokens: 0}` — 9 real calls, zero tokens. Cause: `parseCliTokenUsage` read only top-level `input_tokens`/`output_tokens`, but the current CLI (2.1.237) nests them under `usage` — verified by dumping a real envelope, where none of the four fields it looks for exist at top level. `parseStreamTokenUsage` in the same file already handled nesting; only the non-stream path was stale. Fixed by mirroring it; test watched go red first; live `callClaude` now returns `{input:2, output:4, cacheCreationInput:22617, cacheReadInput:23331}`. **Hench path (A2) still unverified — do not quote hench token numbers yet.** | IN-PROGRESS (A2 + A5 remain) | Butter (Team Nolan) |
| TN-B1 | **`ndx usage` counts 0.027% of the tokens actually spent.** `rex/src/cli/commands/usage.ts:43` and `:60` total `inputTokens + outputTokens` only, and `rex/src/core/item-token-rollup.ts:97-98` accumulates `input`/`output` only — the cache fields are **declared** at `:207-208` and summed nowhere. On Jam's measured payload (2 in / 4 out / 7,318 cache-creation / 14,792 cache-read) that reports **6 of 22,116 tokens**. Blocked twice over: on the ADR's weighting question (cache-read is not priced as input, so a total is a decision not an addition) and on a note to Jarrett/Thomas, since `packages/rex/` is Path C's package. | [`ADR-2026-08-23-butter-savings-measurement-contract.md`](../ADR/ADR-2026-08-23-butter-savings-measurement-contract.md) · [`IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md`](../IMPL/IMPL-2026-08-23-butter-token-measurement-and-path-a-b-seam.md) § A5 | BLOCKED (ADR weighting question) | Butter (Team Nolan) |
| TN-B2 | ~~**We may be paying for a whole agent harness to run one classification.**~~ **RESOLVED 2026-08-23 — not an n-dx defect. Rescoped to a provisioning question for the lead.** Measured `num_turns = 1` on a real classify prompt: n-dx sends one prompt, one turn, passing only `-p - --output-format json --model` (`cli-provider.ts:124-129`). There is no agent loop to remove. The ~95% overhead is the Claude Code CLI loading its own harness as cache-creation per invocation (45,967 tokens cold, `total_cost_usd` $0.367548), which is inherent to CLI-as-backend — and we use the CLI because this machine has no API key. `api-provider.ts` already exists for the other path. **The consequence that does NOT go away:** an avoided call saves ~53,553–267,952 tokens on CLI but only ~2,700–13,700 on API — **~20x** — so every savings figure must state its backend. One unmeasured n-dx-side lever remains, explicitly not claimed: n-dx passes no tool-restriction flags to the CLI. | `Nolan-Agents/ELM-FINDINGS.txt` § 7 | DONE (rescoped — provisioning, not engineering) | Butter (Team Nolan) |
| TN-B3 | **Path A, remaining half — the shared ELM inference wrapper** (load / train / predict / expose confidence). Proposed to Butter on specialisation grounds: foundation-tier plumbing with a confidence contract, adjacent to the `llm-client` work already done, and it serves Paths B and C both so it should not belong to either call site. **Not blocked by `TN-J10`** — plumbing, unit-testable on synthetic data. Awaiting Jam's agreement on the split; Butter offered to swap for Path C if Jam prefers the ML surface. **ADR + IMPL written 2026-08-23.** Decides: ELM tier at the call site (not a registered vendor — closes `SYNC-001` § 5 item 2, open since 08-11); module lives in `@n-dx/llm-client/src/elm/`; probability exposed as a first-class value rather than lost across the text-in/text-out provider seam; trains in-process, seeded 42. **Step 0 needs a second lead** — `@astermind/astermind-community` is a root-only dep and must be added to `packages/llm-client`, which churns `pnpm-lock.yaml`. | [`ADR-2026-08-23-butter-elm-inference-module.md`](../ADR/ADR-2026-08-23-butter-elm-inference-module.md) · [`IMPL-2026-08-23-butter-elm-inference-wrapper.md`](../IMPL/IMPL-2026-08-23-butter-elm-inference-wrapper.md) **UNBLOCKED 2026-08-27 by [`ADR-2026-08-27-butter-prove-before-provisioning.md`](../ADR/ADR-2026-08-27-butter-prove-before-provisioning.md):** the sign-off is needed to *ship* the module in a package, not to *answer the question*. `scripts/` already imports the root ELM dependency (`elm-hello-world.mjs:19`), so the engine gets built and proved as a script-tier prototype first, and the dependency is taken only if it clears the bar — or the request is withdrawn. | [`ADR-2026-08-27-butter-prove-before-provisioning.md`](../ADR/ADR-2026-08-27-butter-prove-before-provisioning.md) · [`IMPL-2026-08-27-butter-elm-prototype-and-telemetry.md`](../IMPL/IMPL-2026-08-27-butter-elm-prototype-and-telemetry.md) § Lane 1 | IN-PROGRESS (prototype route; sign-off pending, not blocking) | Butter (Team Nolan) |
| TN-B4 | **Path C — rex granularity assessment** (`reason.ts:1481`, 3 classes). Proposed to **Jam** on specialisation grounds: a classification-feasibility question with a labels problem, the shape Jam has done twice. **First step is a viability gate, not code** — the call returns the enum *plus* prose (`reasoning`, `issues[]`) rendered to users at `reason.ts:1456-1466`, and an ELM gives the label only. If the prose must stay, Path C is not viable and that is a one-session publishable answer. **Not blocked by `TN-J10`** — needs no LLM, no corpus, no labels. | `Nolan-Agents/Notes/NOTE-nolan-internal-2026-08-23-paths-a-c-split-and-findings-doc.md` § 2 | PENDING (proposed — needs Jam's agreement) | _(proposed for Jam)_ |
| TN-B5 | **Work around `TN-J10` instead of waiting on it.** Train the ELM on the committed 324-row corpus and measure **agreement with the teacher** on the held-out 83. **You do not need a gold set to establish infeasibility:** if the ELM cannot reproduce the teacher, Path B is dead whether or not the teacher is right, and that is a publishable negative costing zero hand-labelled rows. Only if agreement is high does `TN-J10` bind. Also produces the missing input to `TN-J10` itself — a `service`/`utility` confusion matrix quantifies how much teacher inconsistency the ELM inherits, turning a qualitative read of four filenames into a number the leads can weigh. Offline, token-free. | `…-paths-a-c-split-and-findings-doc.md` § 4 | PENDING (proposed for Jam) | _(proposed for Jam)_ |
| TN-B6 | **`CompletionResult` has nowhere to put per-call cost or turn count.** `llm-client/src/types.ts:82-87` is `{text, tokenUsage?}`. The vendor returns `total_cost_usd` and `num_turns` in every CLI envelope and the type cannot carry them — **a schema gap, not a missing read** (Jam's framing, and sharper than my original 'we never read it'). Consequence: the only way to observe either is to bypass `callClaude` and invoke the CLI directly, which is what `elm-token-baseline.mjs` now does. Any dashboard cost figure needs this closed first. Related to `TN-B1`; separable from it. | [`ADR-2026-08-27-butter-prove-before-provisioning.md`](../ADR/ADR-2026-08-27-butter-prove-before-provisioning.md) · [`IMPL-2026-08-27-butter-elm-prototype-and-telemetry.md`](../IMPL/IMPL-2026-08-27-butter-elm-prototype-and-telemetry.md) § Lane 2 | IN-PROGRESS | Butter (Team Nolan) |
| TN-B7 | **Commit a seeded check for dead charSet slots and encoder behaviour.** Syrup verified that `charToOneHot` lowercases before lookup (`astermind.umd.js:762`), so uppercase charSet entries are unreachable and merely widen the input vector — my 08-27 "fix" added 2,080 dead dimensions and **halved the corpus score (9.6% -> 4.8%)**. Reverted. The detection script lives in Syrup's scratchpad and is uncommitted, so per doctrine it does not count. Also cover the `useTokenizer` join-on-empty-string defect (`:771`) and the fact that `useTokenizer:false` throws for text training, so the constraint is recorded in runnable form. | [`NOTE-nolan-internal-2026-08-31-syrup-verified-you-were-right.md`](Notes/NOTE-nolan-internal-2026-08-31-syrup-verified-you-were-right.md) | PENDING | Butter (Team Nolan) |
